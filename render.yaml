services:
  - type: web
    name: sweetpotato-api
    runtime: node
    rootDir: server
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_JWT_AUDIENCE
        value: authenticated
      # Optional: set this in Render dashboard if using MongoDB Atlas
      # - key: MONGODB_URI
      #   value: mongodb+srv://<username>:<password>@<cluster>/<db>
